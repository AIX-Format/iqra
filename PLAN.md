# 🌙 IQRA | خُطَّة التفعيل (Activation Plan) - تحديث

### 📝 المشكلة (Problem)
- محاولة تفعيل Cloudflare و Qdrant واجهت عوائق تقنية:
  1. لا يوجد Docker على الجهاز (تم تأكيده من المستخدم).
  2. لا يوجد اتصال إنترنت في الـ Terminal الخاص بالوكيل (مما يمنع التحميل المباشر).
  3. مشاكل في صلاحيات الوصول لمجلد `.gemini` العالمي.

### 🎯 الهدف المعدل (Revised Goal)
- استخدام بنية Antigravity IDE (Gemini Code Assist) بدلاً من Gemini CLI المستقل.
- الاعتماد على ملف `.ag/mcp.json` لربط الخدمات.
- التجهيز لاستخدام Qdrant Cloud أو Cloudflare Vectorize بدلاً من Qdrant المحلي.

### 🤲 النية (Intention)
تكييف "جسد" IQRA مع البيئة المتاحة دون التنازل عن "الروح" والهدف الأسمى: بناء ذاكرة دلالية تتطور ذاتياً.

### 🚀 الخطوات المنجزة (Completed Steps)
- [x] التحقق من البيئة (Docker, Brew, Python, Network).
- [x] قراءة دستور IQRA وذاكرته الحالية.
- [x] إنشاء مجلد `.ag` وإعداد `.ag/mcp.json` وفقاً لتوصيات المستخدم.
- [x] تحديث `.env.local` بجميع المتغيرات المطلوبة (كعناصر نائبة).

### 🚀 الخطوات التالية (Next Steps)
1. **طلب المدخلات**: على المستخدم تعبئة القيم الحقيقية في `.env.local`.
2. **تفعيل الـ MCP**: بمجرد تعبئة القيم، سيقوم Antigravity بتفعيل الخوادم تلقائياً.
3. **تحويل منطق Qdrant**: تعديل `lib/iqra/qdrant.ts` ليدعم الـ API Key والـ Cloud URL.

---
**تم التحديث بفضل الله**
